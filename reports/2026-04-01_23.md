# Audit & Progress Report - 2026-04-01 23:00

## 1. Ce que j'ai fait ce run
- **Audit initial du codebase**: Prise de connaissance des fichiers existants. Drizzle généré et poussé, tests lancés.
- **Vérification de la mémoire**:
  - Il a été noté que les `products` utilisent `totalStock` et non `stock`.
  - Et les `products` utilisent `salePrice` / `originalPrice`, et non `price`.
  - La route `/api/webhooks/stripe` utilise `stock`, qui n'existe plus dans le schema Drizzle et fait crasher le serveur s'il y a des commandes (`product.stock` n'existe plus, c'est `totalStock`).
  - Idem pour `/api/orders` et `/api/create-payment-intent` qui utilisent `product.price` et `product.stock` au lieu de `salePrice`/`originalPrice` et `totalStock`.

## 2. État actuel du projet (% d'avancement estimé)
- **Avancement estimé**: 75%
- Une grande partie du backend (routes API, schéma) et du frontend (pages) est présente. Mais il y a des incohérences de schéma persistantes depuis un refactoring récent de DB qui empêchent la réservation et le e-commerce de fonctionner correctement, notamment dans le processus de checkout.

## 3. Ce que je prévois de faire au prochain run
- Rien pour l'instant (c'est mon run actuel de correction de bug).

## 4. Blocages éventuels
- Erreurs de compilation Typescript sur les routes API concernées qui doivent être résolues.

## 5. Décisions techniques
- L'adoption stricte du schéma `commerce.ts` pour toutes les transactions (salePrice/originalPrice/totalStock).
