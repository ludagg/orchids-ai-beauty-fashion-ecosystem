## 2025-05-14 - Semantic Accessibility in UI Templates
**Learning:** Landing page templates often use `div` with `cursor-pointer` for interactive elements like play buttons or carousel controls, which are invisible to screen readers and keyboard navigation.
**Action:** Always convert interactive `div` elements to semantic `button` elements and ensure all icon-only buttons have descriptive `aria-label` attributes.
