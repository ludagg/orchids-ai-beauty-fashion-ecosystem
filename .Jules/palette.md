## 2026-07-31 - Partner Registration Form UX & Accessibility
**Learning:** Replaced custom spinners with the unified `Spinner` component. Standardized file and gallery uploads to support keyboard navigation (using `sr-only` instead of `hidden` for file inputs, `focus-within:ring-2` styling on parent containers, and `focus:opacity-100` with `Tooltip` wrapper for removal controls). Fixed label association for Select inputs by adding explicit `id` to `SelectTrigger`.
**Action:** Always prefer `sr-only` for file input elements to ensure they remain tabbable, use `focus-within` on container components, and set matching `id` on triggers to associate labels correctly.
